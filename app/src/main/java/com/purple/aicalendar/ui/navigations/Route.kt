package com.purple.aicalendar.ui.navigations

/**
 * A sealed interface representing a navigation route within the application.
 * Each implementation of this interface corresponds to a specific screen or destination.
 */
interface Route {}